
<%= form_for [@artist, @song] do |f| %>

  <div class="form-group">
    <%f.hidden_field :artist_id, :value => @artist.id%>
    <label> Title of the Song: </label>
     <%= f.text_field :titel, placeholder:'New song title', class: "form-control" %>
  </div>
<% end %>
