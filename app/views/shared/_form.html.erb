
<%= form_for [@artist, @song] do |f| %>

  <div class="form-group">
    <label> Title of the Song: </label>
     <%= f.text_field :titel, placeholder:'New song title', data: {artist: @artist.id}, class: "form-control" %>
  </div>
<% end %>
